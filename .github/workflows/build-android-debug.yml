name: Debug Android Build - DISABLED

# This workflow is disabled to avoid automatic builds
# Use android-only.yml instead for manual Android builds

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
#   workflow_dispatch: # Allow manual trigger

# jobs:
#   debug-android:
#     runs-on: ubuntu-latest
#     name: Debug Android Build
